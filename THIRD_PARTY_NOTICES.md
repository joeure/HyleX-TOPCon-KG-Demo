# Third-party notices

The Inspector UI uses the dependencies declared in
`services/inspector-ui/package.json` and pinned by `package-lock.json`.
Their original licenses remain applicable. The offline package includes a
generated dependency inventory for the closed runtime images.
